---
layout: post
title: "bandit0~24"
date: 2026-04-15
category: "CTF/Wargame"
tags: ["linux","명령어","mission"]
excerpt: "LINUX 명령어 익히기"
---
<div class="bandit-pager" data-bandit-pager>
  <div class="bandit-pager__top">
    <span class="bandit-pager__status" data-bandit-status>Bandit 0 / 24</span>
  </div>
  <div class="bandit-pager__tabs" aria-label="Bandit level navigation">
    <button type="button" data-bandit-tab="0">0</button>
    <button type="button" data-bandit-tab="1">1</button>
    <button type="button" data-bandit-tab="2">2</button>
    <button type="button" data-bandit-tab="3">3</button>
    <button type="button" data-bandit-tab="4">4</button>
    <button type="button" data-bandit-tab="5">5</button>
    <button type="button" data-bandit-tab="6">6</button>
    <button type="button" data-bandit-tab="7">7</button>
    <button type="button" data-bandit-tab="8">8</button>
    <button type="button" data-bandit-tab="9">9</button>
    <button type="button" data-bandit-tab="10">10</button>
    <button type="button" data-bandit-tab="11">11</button>
    <button type="button" data-bandit-tab="12">12</button>
    <button type="button" data-bandit-tab="13">13</button>
    <button type="button" data-bandit-tab="14">14</button>
    <button type="button" data-bandit-tab="15">15</button>
    <button type="button" data-bandit-tab="16">16</button>
    <button type="button" data-bandit-tab="17">17</button>
    <button type="button" data-bandit-tab="18">18</button>
    <button type="button" data-bandit-tab="19">19</button>
    <button type="button" data-bandit-tab="20">20</button>
    <button type="button" data-bandit-tab="21">21</button>
    <button type="button" data-bandit-tab="22">22</button>
    <button type="button" data-bandit-tab="23">23</button>
    <button type="button" data-bandit-tab="24">24</button>
  </div>

  <section class="bandit-page" data-bandit-page markdown="1">

## Bandit 0

![bandit0](/assets/images/bandit/bandit0.png)

### 접속

```bash
ssh bandit0@bandit.labs.overthewire.org -p 2220
```

### 명령어

`ssh`: 원격 서버에 Secure Shell로 접속

`-p 2220`: SSH 포트 지정

### 비밀번호

`bandit0`

### 풀이

```bash
ls
cat readme
```

### 명령어

`ls`: 현재 디렉토리 파일/폴더 목록 출력

`cat <file>`: 파일 내용을 그대로 출력(읽기)

### flag

![bandit0.1](/assets/images/bandit/bandit0-1.png)

해당 문제에서 나온 `flag`가 다음 문제 비밀번호가 된다.

  </section>
  <section class="bandit-page" data-bandit-page markdown="1">

## Bandit 1

![alt text](/assets/images/bandit/image-0.png)

![alt text](/assets/images/bandit/image-1.png)

보면 `-` 파일이 있다. 파일 이름이 `-`로 시작하는 경우 옵션으로 인식하기 때문에

앞에 `./` 경로 지정을 해줘야 한다.

![alt text](/assets/images/bandit/image-2.png)

  </section>
  <section class="bandit-page" data-bandit-page markdown="1">

## Bandit 2

![alt text](/assets/images/bandit/image-3.png)

![alt text](/assets/images/bandit/image-4.png)

난 bandit1처럼 그냥 `-`로 시작하길래 `./-` 'TAB' 자동완성 기능 썼는데 알아서

![alt text](/assets/images/bandit/image-5.png)

이렇게 나오더라

### 왜 why

파일 이름에 공백이 포함되어 있으면 `shell`은 이를 별개 인자로 인식한단다.

이를 방지하기 위해 파일 전체를 `" "`로 감싸거나, 각 공백 앞에 `\`를 붙여야 한다.

  </section>
  <section class="bandit-page" data-bandit-page markdown="1">

## Bandit 3

![alt text](/assets/images/bandit/image-6.png)

![alt text](/assets/images/bandit/image-7.png)

이건 뭐 맨날 `ls -al` 하는게 습관이라

`-a`: (ALL) 숨겨진 파일과 디렉토리를 모두 표시한다.

`-l`: (long) 파일의 권한, 소유자, 그룹, 크기, 수정시간 등 상세정보를 보여준다.

![alt text](/assets/images/bandit/image-8.png)

  </section>
  <section class="bandit-page" data-bandit-page markdown="1">

## Bandit 4

![alt text](/assets/images/bandit/image-9.png)

![alt text](/assets/images/bandit/image-10.png)

`flag`가 있는 파일을 찾아야하는데 이번에도 파일명이 `-`로 시작하기 때문에

`file ./*` 모든 파일의 형식을 확인해보면 `ASCII text`로 된 파일이 있다.

![alt text](/assets/images/bandit/image-11.png)

  </section>
  <section class="bandit-page" data-bandit-page markdown="1">

## Bandit 5

![alt text](/assets/images/bandit/image-12.png)

![alt text](/assets/images/bandit/image-13.png)

이번 문제는 `1033 bytes` 크기의 파일을 찾는 문제

![alt text](/assets/images/bandit/image-14.png)

### 1033c "저 c 는 뭔데요"

`c`는 **bytes**로 지정한다는 의미

`b`: 512바이트 블록 (기본값, 생략 시 적용)

`c`: 1바이트 (Character/Byte)

`w`: 2바이트 (Word)

`k`: 킬로바이트 (1,024바이트)

`M`: 메가바이트 (1,048,576바이트)

`G`: 기가바이트 (1,073,741,824바이트)

정확히 `1033bytes` 인 파일을 찾으라는 명령입니다.

  </section>
  <section class="bandit-page" data-bandit-page markdown="1">

## Bandit 6

![alt text](/assets/images/bandit/image-15.png)

이번 문제는 전체 파일 시스템에서 특정 조건의 파일 찾기이다.

- 소유자: `bandit7`
- 그룹: `bandit6`
- 크기: `33 bytes`

```bash
find -user bandit7 -group bandit6 -size 33c
```

찾아보니까 `denied` 저것들 오류 메시지인데 `2>/dev/null` 이걸 뒤에 추가하면 오류 메시지를 없앨 수 있다.

![alt text](/assets/images/bandit/image-16.png)

이렇게 나오는데 중간에 수상한 것이 보인다.

```bash
cat ./var/lib/dpkg/info/bandit7.password
```

![alt text](/assets/images/bandit/image-17.png)

  </section>
  <section class="bandit-page" data-bandit-page markdown="1">

## Bandit 7

![alt text](/assets/images/bandit/image-18.png)

`data.txt` 파일에서 `millionth` 단어 옆의 비밀번호 찾기

![alt text](/assets/images/bandit/image-19.png)

`grep`: 특정 문자열을 검색하는 명령어

```bash
grep millionth data.txt

또는

cat data.txt | grep millionth
```

편한 걸로 쓰자.

  </section>
  <section class="bandit-page" data-bandit-page markdown="1">

## Bandit 8

![alt text](/assets/images/bandit/image-20.png)

`data.txt`에서 한 번만 나타나는 문자열 찾기

이번 문제는 `sort`, `uniq` 명령어를 사용

![alt text](/assets/images/bandit/image-21.png)

- sort: 파일의 줄을 단위로 정렬
- uniq: 중복된 줄을 하나로 합치거나 생략

  </section>
  <section class="bandit-page" data-bandit-page markdown="1">

## Bandit 9

![alt text](/assets/images/bandit/image-22.png)

`data.txt` 파일에서 여러 `==` 기호로 시작하는 라인에서 비밀번호 찾기

```bash
strings data.txt
# 난 이렇게 쳐서 찾았는데

strings data.txt | grep "^====*"
# 이렇게 옵션을 줘서 찾아도 된다
```

`strings`: 실행 파일이나 데이터 파일 안에 포함된 인쇄 가능한 문자열을 추출해 출력하는 명령어

![alt text](/assets/images/bandit/image-23.png)

```text
라쿤시티도 해봤는데 bandit 꽤 괜찮은 것 같다.
```

  </section>
  <section class="bandit-page" data-bandit-page markdown="1">

## Bandit 10

![alt text](/assets/images/bandit/image-24.png)

`base64` 로 디코딩

![alt text](/assets/images/bandit/image-25.png)

  </section>
  <section class="bandit-page" data-bandit-page markdown="1">

## Bandit 11

![alt text](/assets/images/bandit/image-26.png)

이번 문제는 `rot13`으로 인코딩된 데이터 디코딩하기 !

### rot13

알파벳 문자를 `13` 글자 뒤의 문자로 바꾸는 단순 치환 암호

```bash
cat data.txt | tr 'A-Za-z' 'N-ZA-Mn-za-m'

# tr : 표준 입력으로부터 문자를 읽어 치환하는 명령어

# 'A-Za-z' : 변환할 대상 범위 지정,  모든 대소문자를 의미
# A부터 M까지(1~13번째)는 N부터 Z로 바뀐다
# N부터 Z까지(14~26번째)는 다시 A부터 M으로 바뀐다.
```

![alt text](/assets/images/bandit/image-27.png)

  </section>
  <section class="bandit-page" data-bandit-page markdown="1">

## Bandit 12

![alt text](/assets/images/bandit/image-28.png)

이게 젤 귀찮다 ... 

여러번 압축된 파일 압축 해제 하는 문제다

일단 문제 설명 따라 

```bash
mkdir /tmp/flag
cp data.txt /tmp/flag
cd /tmp/flag
```

`data.txt` 파일은 `hexdump` 로 추출된 텍스트 데이터기 때문에 
`xxd` 를 이용해보자

```bash
xxd -r data.txt /tmp/flag
```

하면 `data` 파일이 생기는데 `file`로 타입을 확인하고 파일명 뒤에 타입에 맞는 확장자를 붙여야 압축을 풀 수 있다.

```bash
mv 원래_이름 바꿀_이름 # 예시 : mv test.txt flag.gz
gzip -d 파일이름
bz2 -d 파일이름
tar -xvf 파일이름
```

그렇게 노가다를 하면 `ASCII text` 타입 파일이 생긴다...

![alt text](/assets/images/bandit/image-29.png)

  </section>
  <section class="bandit-page" data-bandit-page markdown="1">

## Bandit 13

![alt text](/assets/images/bandit/image-30.png)

이 문제는 `SSH` 개인 키를 사용해 `bandit14`에 로그인하는 문제다

`ssh -i file_name bandit14@~~~~`머시기머시기 

여기서 `-i file` 은 비밀번호 대신 이 파일을 증거로 증거로 제출할게요~ 이다.

![alt text](/assets/images/bandit/image-31.png)

근데 안되노

이게 그 뭐지 대충 계정에 `ssh`권한이 없어서 그렇다, 설정이 그렇게 되있다라고 했던거 같은데

그리고 `ssh` 개인키로 들어갈라면 파일 권한을 소유자만 읽고 쓸 수 있게 변경해야 한다 라고 하더라고요

쨋든 그래서 전 `scp` 로 파일을 가져왔습니다. 

```bash
scp -P 2220 bandit13@bandit.labs.overthewire.org:~/sshkey.private ~/sshkey.private
# 이렇게 가져오고

chmod 600 ~/sshkey.private 
# 권한 변경해주고 
```
하고 다시 접속해보면 

`good !`

문제 설명대로 

```bash
cat /etc/bandit_pass/bandit14

```

![alt text](/assets/images/bandit/image-32.png)

  </section>
  <section class="bandit-page" data-bandit-page markdown="1">

## Bandit 14

![alt text](/assets/images/bandit/image-33.png)

`bandit14`의 `pw`를 `30000` 포트에 전송하라네요

```bash
telnet localhost 30000 #하고 pw 전송
    or
nc localhost 30000 # |  옵션 붙여서 해도 되네
```

### telnet vs nc

요 둘의 차이점은 

| `telnet` | `nc` | 
| :----: | :----: | 
| 텍스트 기반 원격 터미널 접속 | 네트워크 분석 및 접속 | 
| 주로 TCP만 지원 | TCP UDP 모두 지원 |
| 데이터 전송 위주 | 서버 모드(-l), 포트스캔 등 다양함 |

![alt text](/assets/images/bandit/image-34.png)

  </section>
  <section class="bandit-page" data-bandit-page markdown="1">

## Bandit 15

![alt text](/assets/images/bandit/image-35.png)

`openssl` 이용해서 `30001`  pw 전송

```bash
openssl s_client -connect localhost:30001
```

### openssl s_client 가 뭐냐 

`SSL/TLS` 암호화 통신을 가능하게 해주는 강력한 도구

`nc` 나 `telnet`이 평문으로 데이터를 주고받는다면 `openssl s_client`는 데이터를 암호화해서 안전하게 주고 받는다.

![alt text](/assets/images/bandit/image-36.png)

  </section>
  <section class="bandit-page" data-bandit-page markdown="1">

## Bandit 16

![alt text](/assets/images/bandit/image-37.png)

이번 문제는 `localhost 31000 ~ 32000` 범위 내에서 SSL 을 지원하는 포트 찾기

```bash
nmap localhost -p 31000-32000
 # 이래 하면 범위 내에 포트들이 몇개 뜨는데 한개씩 접속해서 비번 쳐보면 된다.

openssl s_client -connect localhost:[port] -quiet  # -quiet 이게 불필요한 메시지를 줄이는 거 

 # 하다가 보면 
 # RSA key 가 나오는데 아까 문제에 나왔죠
 # 그대로 하면 됩니다

vim file_name # 에다가 복사해서 넣고 권한 설정 600 해주고 ssh 다음걸로 하면 됩니다
```

![alt text](/assets/images/bandit/image-38.png)

접속해서 비번 알아내기

  </section>
  <section class="bandit-page" data-bandit-page markdown="1">

## Bandit 17

![alt text](/assets/images/bandit/image-39.png)

이번 문제는 비교

```bash
diff fileold filenew 
 # -u 는 선택
```

하면 나옵니다.

![alt text](/assets/images/bandit/image-40.png)

  </section>
  <section class="bandit-page" data-bandit-page markdown="1">

## Bandit 18

![alt text](/assets/images/bandit/image-41.png)

문제 안읽고 접속했다가 

`어우 씨 머고 왜 안되노 ;; `

문제를 꼭 읽고 푸세요

이 문제는 `~./bashrc`가 강제로 로그아웃 되는데

찾아보니까 ssh 접속할 떄 마지막에 `"cat slfjeifi32i32"` 이런식으로 명령어가 되더라고요

뭔 파일 있는지 모르니까

![alt text](/assets/images/bandit/image-42.png)

함 갈겨주고

연결

![alt text](/assets/images/bandit/image-43.png)

  </section>
  <section class="bandit-page" data-bandit-page markdown="1">

## Bandit 19

![alt text](/assets/images/bandit/image-44.png)

이번 문제는 `setuid`를 이용해서 푸는 문제

![alt text](/assets/images/bandit/image-45.png)

여기 보면 뭐가 좀 이상하죠?

권한자리 보면 `x` 자리에 이상한게 껴있습니다.

저거 수상하니까 `./bandit20-do id` 해보면

![alt text](/assets/images/bandit/image-46.png)

저 `euid`는 명령어를 실행하는 순간은 시스템이 나를 `bandit20`으로 보이게 하는 거라네요

`EUID` = 파일 주인의 권한을 빌려줄게 ! 살짝 이런 느낌?

`password` 어디있는지 기억나시죠?

![alt text](/assets/images/bandit/image-47.png)

여기서 중요한 점은 권한이 상승된 실행 파일이 시스템에 존재할 때, 이를 어떻게 악용할 수 있는가를 보여준다.

만약 `root` 소유로 되있고, 아무 명령어나 실행할 수 있게 되있으면 

일반 사용자가 `sudo` 없이도 시스템 전체를 장악할 수 있게 된다. 

```text
문제 푸는 거 보다 블로그 올리는 게 더 힘들어요
ㅠ

```

  </section>
  <section class="bandit-page" data-bandit-page markdown="1">

## Bandit 20

![alt text](/assets/images/bandit/image-48.png)

`setuid` 바이너리가 `localhost:suconnect` 포트에 연결하고 `password` 전송하고 응답 받기

이번 문제는 터미널을 2개 열어야 한다

아까 `telnet` `nc` 비교 해놨죠?

`nc` 에는 서버 모드가 있습니다.

터미널1에는 `nc -lp 9999` 과 `password` 전송해놓고 

터미널2에는 `./suconnect 9999` 를 보내면

![alt text](/assets/images/bandit/image-49.png)

![alt text](/assets/images/bandit/image-50.png)

짜잔

  </section>
  <section class="bandit-page" data-bandit-page markdown="1">

## Bandit 21

![alt text](/assets/images/bandit/image-51.png)

`cron job` 분석 !

여기도 문제 설명에 나와있죠??

`/etc/cron.d` 를 찾아봅시다

![alt text](/assets/images/bandit/image-52.png)

지금 21이니까 `cronjob_bandit22` 확인해보면 되겠죠

```bash
bandit21@bandit:/etc/cron.d$ cat cronjob_bandit22
@reboot bandit22 /usr/bin/cronjob_bandit22.sh &> /dev/null
* * * * * bandit22 /usr/bin/cronjob_bandit22.sh &> /dev/null
```

저 파일을 쓰레기통에 버린다고 되있네요 

뭔지 함 보자

![alt text](/assets/images/bandit/image-53.png)

  </section>
  <section class="bandit-page" data-bandit-page markdown="1">

## Bandit 22

![alt text](/assets/images/bandit/image-54.png)

이번 문제도 `cron`

확인해봅시다

![alt text](/assets/images/bandit/image-55.png)

코드 보니까 `mytarget`을 얻어야 하네요 

코드 그대로 긁어와서 `bandit23` 만 넣어주고 

```bash
echo I am user bandit23 | md5sum | cut -d ' ' -f 1
```

![alt text](/assets/images/bandit/image-56.png)

  </section>
  <section class="bandit-page" data-bandit-page markdown="1">

## Bandit 23

![alt text](/assets/images/bandit/image-57.png)

또 `cron` 

한번 확인해보자

![alt text](/assets/images/bandit/image-58.png)

```bash
#!/bin/bash
myname=$(whoami) # 실행 주체: bandit24
cd /var/spool/"$myname"/foo || exit

for i in * .*; do
    if [ "$i" != "." ] && [ "$i" != ".." ]; then
        owner="$(stat --format "%U" "./$i")"
        # 파일 소유자가 bandit23(나)이면 실행!
        if [ "${owner}" = "bandit23" ] && [ -f "$i" ]; then
            timeout -s 9 60 "./$i"
        fi
        rm -rf "./$i" # 실행 후 즉시 삭제
    fi
done

```

`bandit24` 권한으로 실행되는 스크립트가 `/var/spool/bandit24/foo` 안의 파일을 무조건 실행

내가 파일 만들어서 저 폴더 안에 넣으면 실행되겠네?

![alt text](/assets/images/bandit/image-59.png)

/tmp 에 폴더 만들고 `777` 권한 다 줘버리기

```bash
#!/bin/bash
cat /etc/bandit_pass/bandit24 > /tmp/qlqjs/qlqjs.txt
```

![alt text](/assets/images/bandit/image-60.png)

파일명 `.sh` 로 바꿔주고 보냈는데 몇분 지나도 안오노 ;;

파일에 권한을 안줬네 ㅎ

권한 주고 다시 보내버리고 조금 기다리면 파일이 생긴다

![alt text](/assets/images/bandit/image-61.png)

  </section>
  <section class="bandit-page" data-bandit-page markdown="1">

## Bandit 24

![alt text](/assets/images/bandit/image-62.png)

이번 문제는 `bruteforce`로 4자리 PIN 찾기 `localhost:30002`

`30002` 포트에 접속해서 `bandit24` 의 `pw` 와 4자리 `pin` 코드를 한 줄에 같이 보내야 한다

일단 `list`를 만들자

```bash
for i in {0000..9999}; do
    echo "[pswd] $i" >> list.txt
done
```

하고 

`./pin` 하면 `list.txt`가 생긴다

```bash
cat list.txt | nc localhost 30002
```

했는데 어우 `Wrong`

![alt text](/assets/images/bandit/image-63.png)

치워버리자

```bash
cat list.txt | nc localhost 30002 | grep -v "Wrong!"
# -v 옵션은 해당 문자열이 포함되지 않은 줄만 보여준다
```

![alt text](/assets/images/bandit/image-64.png)

## 명령어 정리

| `명령어` | 설명 |
| :----: | :----: |
| `cat` | 파일 내용 출력 |
| `ls -al` | 숨겨진 파일 포함 모든 파일 표시 |
| `file` | 파일 타입 확인 |
| `find` | 특정 조건의 파일 탐색 |
| `grep` | 문자열 검색 |
| `strings` | 바이너리에서 문자열 추출 |
| `sort` | 정렬 |
| `uniq` | 중복 제거/찾기 |
| `base64` | base64 인코딩/디코딩 |
| `tr` | 문자 변환 |
| `gzip` / `bzip -d` / `tar -xvf` | 파일 압축 해제 |
| `ssh` | 원격 접속 |
| `nc`/`telnet` | 네트워크 통신 |
| `openssl s_client` | SSL 연결 |
| `diff` | 파일 비교 |

```text
시간 될 때 마다 풀어서 싹 다 정리 해놔야겠다

```

  </section>
  <div class="bandit-pager__bottom">
    <button type="button" class="bandit-pager__button" data-bandit-prev>&#51060;&#51204;</button>
    <button type="button" class="bandit-pager__button" data-bandit-next>&#45796;&#51020;</button>
  </div>
</div>

<style>
  .bandit-pager {
    display: grid;
    gap: 1.25rem;
  }

  .post-toc {
    display: none !important;
  }

  .bandit-pager__top,
  .bandit-pager__bottom {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.85rem;
    border: 1px solid rgba(45, 103, 66, 0.16);
    border-radius: 8px;
    background: rgba(238, 248, 240, 0.72);
  }

  .bandit-pager__top {
    justify-content: center;
  }

  .bandit-pager__bottom {
    justify-content: space-between;
  }

  .bandit-pager__status {
    color: #174326;
    font-size: 0.95rem;
    font-weight: 800;
    text-align: center;
  }

  .bandit-pager__button,
  .bandit-pager__tabs button {
    min-height: 40px;
    border: 1px solid rgba(45, 103, 66, 0.18);
    border-radius: 8px;
    background: #fafffb;
    color: #174326;
    cursor: pointer;
    font: inherit;
    font-weight: 800;
    transition: background 0.18s ease, border-color 0.18s ease, transform 0.18s ease;
  }

  .bandit-pager__button {
    padding: 0 1rem;
  }

  .bandit-pager__tabs {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(42px, 1fr));
    gap: 0.45rem;
  }

  .bandit-pager__tabs button {
    padding: 0;
  }

  .bandit-pager__button:hover,
  .bandit-pager__tabs button:hover,
  .bandit-pager__button:focus-visible,
  .bandit-pager__tabs button:focus-visible {
    border-color: rgba(63, 180, 116, 0.55);
    background: #eef8f0;
    transform: translateY(-1px);
  }

  .bandit-pager__button:disabled {
    cursor: not-allowed;
    opacity: 0.45;
    transform: none;
  }

  .bandit-pager__tabs button.is-active {
    background: #174326;
    color: #f6fff7;
    border-color: #174326;
  }

  .bandit-page[hidden] {
    display: none;
  }

  body.dark-mode .bandit-pager__top,
  body.dark-mode .bandit-pager__bottom {
    border-color: rgba(133, 191, 148, 0.18);
    background: rgba(16, 28, 20, 0.76);
  }

  body.dark-mode .bandit-pager__status {
    color: #d7f2dd;
  }

  body.dark-mode .bandit-pager__button,
  body.dark-mode .bandit-pager__tabs button {
    border-color: rgba(133, 191, 148, 0.2);
    background: rgba(15, 23, 18, 0.92);
    color: #d7f2dd;
  }

  body.dark-mode .bandit-pager__tabs button.is-active {
    background: #63c174;
    color: #08170f;
    border-color: #63c174;
  }

  @media (max-width: 640px) {
    .bandit-pager__tabs {
      grid-template-columns: repeat(5, minmax(0, 1fr));
    }
  }
</style>

<script>
  (() => {
    const pager = document.querySelector('[data-bandit-pager]');

    if (!pager) {
      return;
    }

    const pages = Array.from(pager.querySelectorAll('[data-bandit-page]'));
    const tabs = Array.from(pager.querySelectorAll('[data-bandit-tab]'));
    const status = pager.querySelector('[data-bandit-status]');
    const prevButtons = Array.from(pager.querySelectorAll('[data-bandit-prev]'));
    const nextButtons = Array.from(pager.querySelectorAll('[data-bandit-next]'));
    const initialLevel = Number(new URLSearchParams(window.location.search).get('level'));
    let currentIndex = Number.isInteger(initialLevel) ? initialLevel : 0;

    const maxIndex = Math.min(pages.length, tabs.length) - 1;
    const clampIndex = (index) => Math.min(Math.max(index, 0), maxIndex);

    const showPage = (index, shouldScroll = false) => {
      currentIndex = clampIndex(index);

      pages.forEach((page, pageIndex) => {
        page.hidden = pageIndex !== currentIndex;
      });

      tabs.forEach((tab) => {
        tab.classList.toggle('is-active', Number(tab.dataset.banditTab) === currentIndex);
        tab.setAttribute('aria-current', Number(tab.dataset.banditTab) === currentIndex ? 'page' : 'false');
      });

      prevButtons.forEach((button) => {
        button.disabled = currentIndex === 0;
      });

      nextButtons.forEach((button) => {
        button.disabled = currentIndex === maxIndex;
      });

      if (status) {
        status.textContent = 'Bandit ' + currentIndex + ' / ' + maxIndex;
      }

      const url = new URL(window.location.href);
      url.searchParams.set('level', currentIndex);
      window.history.replaceState(null, '', url);

      if (shouldScroll) {
        pager.scrollIntoView({ behavior: 'smooth', block: 'start' });
      }
    };

    tabs.forEach((tab) => {
      tab.addEventListener('click', () => showPage(Number(tab.dataset.banditTab), true));
    });

    prevButtons.forEach((button) => {
      button.addEventListener('click', () => showPage(currentIndex - 1, true));
    });

    nextButtons.forEach((button) => {
      button.addEventListener('click', () => showPage(currentIndex + 1, true));
    });

    showPage(currentIndex);
  })();
</script>
